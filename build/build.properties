dir.project.name = msOAuth-php

dir.root = ${basedir}/..
dir.dist = ${dir.root}/dist
dir.project = ${dir.root}/${dir.project.name}

# [Symfony]
symfony.dir.src.name = src
symfony.bundle.dir.source.name = Ms

symfony.dir.root = ${dir.project}
symfony.dir.src = ${symfony.dir.root}/${symfony.dir.src.name}
symfony.bundle.dir.src = ${symfony.dir.src}/${symfony.bundle.dir.source.name}

# [ZIP]
zip.destfile.name = ${dir.project.name}

zip.basedir = ${dir.project}
zip.destfile = ${dir.dist}/${zip.destfile.name}.zip
zip.includes = ${symfony.dir.src.name}/${symfony.bundle.dir.source.name}/**/*